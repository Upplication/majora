'use strict';

describe('connect to /register', function () {
    it('should return TOKEN', function () {
        var obj = "{success: true; token:1234; expitation: 2015-01-30}";
        expect(1+1).toBe(2);
    });
});